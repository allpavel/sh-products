import { HeaderTop } from "../HeaderTop/HeaderTop";
import { HeaderNav } from "../HeaderNav/HeaderNav";

export const Header = () => {
  return (
    <header>
      <HeaderTop />
      <HeaderNav />
    </header>
  );
};
